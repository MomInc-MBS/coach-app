// Rank 2b (D1): the one place that names where optional-art packs are served
// from. Every pack consumer imports this instead of hardcoding a host, so
// moving to a different static host later is a one-line change.
export const PACK_HOST_BASE_URL = 'https://packs.mominc.online';
